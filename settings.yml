use_default_settings: true

general:
  instance_name: "SearXNG"
  debug: false

server:
  secret_key: "changeme"    # overridden at runtime by SEARXNG_SECRET env var
  limiter: false
  image_proxy: true

search:
  formats:
    - html
    - json

ui:
  static_use_hash: true